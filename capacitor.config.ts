import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cafetoria.app',
  appName: 'Cafetoria',
  webDir: 'dist/cafe-order-app/browser'
};

export default config;
